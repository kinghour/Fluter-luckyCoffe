import 'package:flutter/material.dart';

class ModeStless extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }

  const ModeStless({Key key}) : super(key: key);
}
